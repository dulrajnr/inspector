---
"@mcpjam/sdk": patch
---

Wire eval case intent labels and per-stage measurements through SDK reporting and
suite-file contracts. Deploy the backend storage/ingest support before enabling
these reporters against a production endpoint.
